mixin jumbotron(index)
    .jumbotron
        picture.jumbotron__background
            source(srcset=`${imagePath}${jumbotron[index].desktop}.jpg`, class="jumbotron__image", media="(min-width: 768px)")
            source(srcset=`${imagePath}${jumbotron[index].tablet}.jpg`, class="jumbotron__image", media="(min-width: 480px)")
            img(src=`${imagePath}${jumbotron[index].movil}.jpg`, class="jumbotron__image", alt="sofipo")
        .jumbotron__box
            .jumbotron__description
                .jumbotron__header
                    h1.jumbotron__title= `${jumbotron[index].title}`
                    h2.jumbotron__subtitle= `${jumbotron[index].subtitle}`
                p.jumbotron__body= `${jumbotron[index].body}`
                if `${jumbotron.buttonone}` && `${jumbotron.buttontwo}`
                    .jumbotron__buttons
                            a(href=`#${jumbotron[index].buttonone.split(' ').join('-')}`, class="button")= `${jumbotron[index].buttonone}`
                            a(href=`#${jumbotron[index].buttontwo.split(' ').join('-')}`, class="button")= `${jumbotron[index].buttontwo}`

mixin featured_right(index)
    .featured
        .featured__wrap
            img(src=`${imagePath}${featured[index].image}.jpg`, alt="misión")
        .featured__wrap
            .featured__header
                img(src=`${imagePath}icon-${featured[index].icon}.svg`, class="featured__icon", alt="misión")
                h2.featured__title= `${featured[index].title}`
            p.featured__body= `${featured[index].body}`

mixin featured_left(index)
    .featured.featured--left
        .featured__wrap
            img(src=`${imagePath}${featured[index].image}.jpg`, alt="misión")
        .featured__wrap
            .featured__header
                img(src=`${imagePath}icon-${featured[index].icon}.svg`, class="featured__icon", alt="misión")
                h2.featured__title= `${featured[index].title}`
            p.featured__body= `${featured[index].body}`

mixin margin_height(height)
    div(style=`max-width: 100%; height: ${height}rem;`)